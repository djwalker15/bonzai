# Bonzai environment — copy to `.env` and fill in. Never commit real values.
# The MVP runs fully offline (local store) without these; set them to enable
# shared Supabase-backed family data.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
